[project]
name = "predict"
version = "0.1.0"
description = "predict everything"
requires-python = ">=3.13"
dependencies = []


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.hatch.build.targets.wheel]
packages = ["predict"]
